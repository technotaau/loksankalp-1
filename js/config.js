/* लोकसंकल्प — form endpoint.
 *
 * Paste the Google Apps Script Web App URL between the quotes, then commit.
 * It ends in /exec. Setup steps: docs/FORMS.md
 *
 * Left empty, every form still works but only shows its confirmation panel —
 * nothing is saved anywhere, and the panel says so.
 */
window.LOKSANKALP_FORM_ENDPOINT = '';
